kind: Service
apiVersion: v1
metadata:
  name: frontend
  # labels:
  #   app: frontend-service
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: frontend
