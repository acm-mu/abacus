apiVersion: v1
kind: Service
metadata:
  name: node-service
  labels:
    app: node-service

spec:
  selector:
    app: node-pod
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
